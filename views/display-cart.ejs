<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Your Cart</title>
  <style>
    body {
      font-family: Arial, sans-serif;
    }
    h1 {
      text-align: center;
    }
    table {
      width: 90%;
      margin: 30px auto;
      border-collapse: collapse;
    }
    th, td {
      padding: 12px;
      border: 1px solid #ccc;
      text-align: center;
    }
    th {
      background-color: #f4f4f4;
    }
    .summary {
      width: 90%;
      margin: 20px auto;
      display: flex;
      justify-content: space-between;
    }
    .btn {
      background-color: #007bff;
      color: white;
      padding: 10px 20px;
      border: none;
      cursor: pointer;
    }
    .btn:hover {
      background-color: #0056b3;
    }
    a {
      display: block;
      text-align: center;
      margin-top: 20px;
    }
  </style>
</head>
<body>
  <h1>Your Cart</h1>

  <% if (cartItems.length === 0) { %>
    <p style="text-align:center;">Your cart is empty.</p>
  <% } else { %>
    <form action="/place-order" method="POST">
      <table>
        <thead>
          <tr>
            <th>Product ID</th>
            <th>Product Name</th>
            <th>Price (₹)</th>
            <th>Quantity</th>
            <th>Total (₹)</th>
            <th>Stock Status</th>
          </tr>
        </thead>
        <tbody>
          <% cartItems.forEach(item => { %>
            <tr>
              <td><%= item.product_id %></td>
              <td><%= item.product_name %></td>
              <td><%= item.price %></td>
              <td><%= item.quantity %></td>
              <td><%= item.price * item.quantity %></td>
              <td style="<%= 'color:' + (item.stock >= item.quantity ? 'green' : 'red') %>">
                <%= item.stock >= item.quantity ? 'In Stock' : 'Out of Stock' %>
              </td>
            </tr>
          <% }) %>
        </tbody>
      </table>

      <div class="summary">
        <div><strong>Total Cart Price: ₹<%= totalCartPrice %></strong></div>
        <button class="btn" type="submit">Place Order</button>
      </div>
    </form>
  <% } %>

  <a href="/dashboard">← Back to Dashboard</a>
</body>
</html>